<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <include layout="@layout/include_toolbar"/>

    <RadioGroup
        android:id="@+id/rg_my_ask_tab"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/white"
        android:orientation="horizontal"
        >

        <RadioButton
            android:id="@+id/rb_my_ask_question"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:checked="true"
            android:drawableBottom="@drawable/shape_tab_indicator"
            android:gravity="center"
            android:text="@string/text_my_ask"
            android:textColor="@color/text_tab"
            android:textSize="@dimen/text_size_body_material"
            />

        <RadioButton
            android:id="@+id/rb_my_ask_answer"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:checked="false"
            android:drawableBottom="@drawable/shape_tab_indicator"
            android:gravity="center"
            android:text="@string/text_my_answer"
            android:textColor="@color/text_tab"
            android:textSize="@dimen/text_size_body_material"
            />

    </RadioGroup>

    <com.jude.easyrecyclerview.EasyRecyclerView
        android:id="@+id/recycle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

</LinearLayout>